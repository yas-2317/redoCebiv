# docs

プロダクト運用・設計・引き継ぎ用の資料をまとめる場所。

## Structure

- `design/`: 設計書、ADR、実装仕様
- `handoffs/`: 作業ハンドオフ、レビュー引き継ぎ
- `marketing/`: ローンチ準備、運用、SNS戦略

アプリ本体の実装は `src/`、公開アセットは `public/`、Supabase 関連は `supabase/` に置く。
